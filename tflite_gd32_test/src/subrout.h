void t5omsi(void);                  //Initialize T5 to a one (1) ms counter.
int t5expq(void);                   //Returns T if T5 has expired else F.

int map(int start, int oldMin, int newMin, int oldMax, int newMax);